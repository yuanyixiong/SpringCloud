server:
  port: 10013
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: eureka-config
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/Innovaee2018/SpringCloud
          searchPaths: config